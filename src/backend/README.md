# Backend

This folder contains the Fastify backend with Drizzle ORM for the Wishlist App.

- `index.ts` — Fastify server entrypoint
- `db/` — Drizzle/Postgres database client and schema
- `routes/` — Fastify route modules

The frontend lives in `src/app`, `src/components`, `src/context`, and `src/lib`.
